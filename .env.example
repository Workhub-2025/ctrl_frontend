# Environment variables for ctrl_frontend (Next.js 15)
# Copy this file to .env.local and fill in the actual values

# ── Server ──────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3000
HOSTNAME=0.0.0.0

# ── NextAuth ─────────────────────────────────────────────────────────────────
# NEXTAUTH_URL must match the exact origin used in the browser.
# Production: https://your-frontend.pages.dev
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here
# JWT_SECRET belongs on Strapi (ctrl_backend) only — not used by FrontEnd src.

# ── Upstash (recommended for production) ─────────────────────────────────────
# Shared login lockout, rate limits, and audit log persistence.
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Optional session tuning (seconds)
# SESSION_MAX_AGE_SECONDS=2592000
# SESSION_IDLE_MAX_AGE_SECONDS=604800

# ── Strapi backend URLs ───────────────────────────────────────────────────────
# STRAPI_API_URL       — server-side only (SSR / API routes).
#                        In Docker: http://strapi:1337/api
#                        In development: http://127.0.0.1:1337/api
#                        On Vercel/production: https://be.ctrl-assess.co.uk/api (public URL — never 192.168.x.x)
STRAPI_API_URL=http://127.0.0.1:1337/api
# NEXT_PUBLIC_STRAPI_API_URL — exposed to the browser (client components).
#                              Must be a public URL in production.
NEXT_PUBLIC_STRAPI_API_URL=http://127.0.0.1:1337/api

# ── Strapi API tokens ─────────────────────────────────────────────────────────
# Full-access token — used for admin operations (server-side only, never NEXT_PUBLIC_).
# Generate in Strapi Admin → Settings → API Tokens.
STRAPI_API_FULL_ACCESS_TOKEN=your-strapi-full-access-token-here
# Read-only token — used for public/low-privilege reads (server-side only).
STRAPI_API_READONLY_TOKEN=your-strapi-readonly-token-here

# ── Deployment flags ─────────────────────────────────────────────────────────
# Set to "true" when deploying to Cloudflare Pages.
# Enables static export (output: 'export') and disables image optimisation.
# CLOUDFLARE_PAGES=true

# ── Stripe billing ─────────────────────────────────────────────────────────────
# Secret key — required to create Checkout sessions (admin send-invoice / renewals).
# Test key from Stripe Dashboard → Developers → API keys.
STRIPE_SECRET_KEY=sk_test_...
# Webhook signing secret — required for POST /api/webhooks/stripe after payment.
# Local: stripe listen --forward-to localhost:3000/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_...
# Optional: reuse a fixed Customer Portal configuration (otherwise auto-created with cancel/update disabled).
# STRIPE_BILLING_PORTAL_CONFIGURATION_ID=bpc_...
# Shared secret for webhook → Strapi billing internal endpoints (match ctrl_backend .env).
BILLING_INTERNAL_SECRET=your-billing-internal-secret-here
# Public app URL for Stripe success/cancel redirects.
NEXT_PUBLIC_APP_URL=http://localhost:3000
