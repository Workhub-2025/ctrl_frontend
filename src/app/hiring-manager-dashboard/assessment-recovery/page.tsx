import { notFound } from "next/navigation";

/** Recovery is owned by the candidate report and administrative workspace. */
export default function RemovedHiringManagerRecoveryPage() {
  notFound();
}
